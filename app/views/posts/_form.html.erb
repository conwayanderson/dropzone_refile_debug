<%= form_for @post, :html=> {class:"dropzone", id:"my-awesome-dropzone"} do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.attachment_field :images_files, multiple: true, direct:true, presigned:true, hidden:true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
